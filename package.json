{
	"name": "menu-ghost",
	"version": "1.0.0",
	"description": "Target menu items by role, device, schedule, and campaign rules from a native conditions panel.",
	"author": "Reza Sarailoo",
	"license": "GPL-2.0",
	"scripts": {
		"build": "wp-scripts build",
		"build:production": "composer install && composer phpcs && npm run lint:js && npm run lint:css && composer test && npm run test:unit && composer language && && npm install npm run build && npm run plugin-zip",
		"check-engines": "wp-scripts check-engines",
		"check-licenses": "wp-scripts check-licenses",
		"format": "wp-scripts format",
		"lint:css": "wp-scripts lint-style --fix",
		"lint:js": "wp-scripts lint-js --fix",
		"lint:md:docs": "wp-scripts lint-md-docs",
		"lint:pkg-json": "wp-scripts lint-pkg-json",
		"packages-update": "wp-scripts packages-update",
		"plugin-zip": "wp-scripts plugin-zip",
		"start": "wp-scripts start",
		"test:unit": "wp-scripts test-unit-js"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.4.0"
	}
}
